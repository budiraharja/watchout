<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/tv_toast_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:fontFamily="sans-serif"
        android:gravity="center_horizontal"
        android:padding="@dimen/layout_padding"
        android:background="@color/candid_setting_background_color"
        android:text="@string/candid_setting"
        android:textAlignment="center"
        android:textColor="@color/candid_text_close_color"
        android:textSize="@dimen/layout_text_size"
        android:visibility="gone" />

</RelativeLayout>